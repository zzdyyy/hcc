//opt.c
//功能性测试：ascii码转换器，输出一张ascii表，根据输入的ascii码或字符进行转换

//测试要点：
//类型相关测试（尤其是字符的转换和输出）

int x, y, z, b, c;
int dag(int a, int v, int vv, int d)
{
    int e,f;
    a=-c;
    d=b*a;
    e=-c;
    c=b*e;
    f=d+c;
    x=f;
    return (x);
}

void dataflow()
{
    int a,b,c,d,e;
    c=(a+b)+(b+a);
    int i,j,k;
    switch(k)
    {
        case 1:i=k+1;
        default:j=k+2;
    }
    switch(k)
    {
        case 2:printf(i);
        default:printf(j);
    }
}

void g()
{}

void phasees()
{
    int i,j;
    i=0;
    while(i<10)
        i=i+1;
    j=1;
    while(j<23)
        j=j+2;
    i=0;
    while(i<55)
        i=i+0;
}

void main()
{
    int a;
    int b;
    int c;
    a=1;
    b=2;
    c=(a+b)+(a+b)-(a+b)/(a+b)*(a+b);
    dag(c-a, c-a, c-a, c-a);
    while(a)
    {
        c=a;
        a=b;
        b=c;
    }
    a=x;
    b=a;
    g();
    c=a;
    printf(a,b,b);
}